<UserControl x:Class="HelloWpf.Pages.InventoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="28" FontWeight="Bold" Foreground="#111827" Text="Inventory" />
        <WrapPanel Grid.Row="1" Margin="0,14,0,14">
            <Button Content="Items" Padding="12,7" Margin="0,0,10,0" Background="#B45309" Foreground="White" BorderBrush="#B45309" />
            <Button Content="Stock Transfer" Padding="12,7" Margin="0,0,10,0" Background="White" BorderBrush="#D1D5DB" />
            <Button Content="Stock Adjustment" Padding="12,7" Background="White" BorderBrush="#D1D5DB" />
        </WrapPanel>

        <Border Grid.Row="2" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8" Padding="12">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="SemiBold" Text="Inventory Snapshot" />
                <TextBlock Margin="0,8,0,0" Foreground="#4B5563" Text="Item: Printer Paper | In Stock: 420" />
                <TextBlock Margin="0,6,0,0" Foreground="#4B5563" Text="Item: USB Cable | In Stock: 85" />
                <TextBlock Margin="0,6,0,0" Foreground="#4B5563" Text="Item: Mouse | In Stock: 26 (Low)" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
